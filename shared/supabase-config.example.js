/**
 * Clés publiques du projet Supabase (Settings → API).
 * url + anonKey : Project URL et anon public key.
 * La anon key n’est PAS la service_role (jamais dans le navigateur).
 */
window.DEVIS_SUPABASE = {
  url: "https://YOUR_PROJECT.supabase.co",
  anonKey: "YOUR_ANON_KEY",
  artisanPublicId: "art_demo_01",
  // Optionnel : identifiant public de l’atelier par défaut (page / widget sans ?a=)
};
